def mark_attendance(emp_id, date, status):
    print(f"[STUB] mark_attendance called with emp_id={emp_id}, date={date}, status={status}")

def get_attendance_for_month(emp_id, month):
    print(f"[STUB] get_attendance_for_month called with emp_id={emp_id}, month={month}")
    return 0
